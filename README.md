# springboot-demo

A minimal Spring Boot application used to simulate a git commit and remote push workflow.

## Run locally

```bash
mvn spring-boot:run
```

Then visit:

- `http://localhost:8080/`
- `http://localhost:8080/health`
